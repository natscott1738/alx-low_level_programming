#include "main.h"

/**
 * _abs - computes the abs value of an int variable
 * @n: is the variable with the value to be checked
 *
 * Return: returns
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
